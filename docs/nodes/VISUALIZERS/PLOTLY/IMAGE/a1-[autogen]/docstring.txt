The IMAGE node creates a Plotly image visualization for a given
    input data container type of image.

    Parameters:
    -----------
    None

    Supported DC types:
    ------------------
    `image`
