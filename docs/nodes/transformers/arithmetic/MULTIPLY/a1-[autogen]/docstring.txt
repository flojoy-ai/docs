The MULTIPLY node multiplies 2 numeric arrays, matrices, dataframes, or constants element-wise. If 2 arrays or matrices of different sizes are multiplied, the output will be the size of the larger array or matrix with only the overlapping elements changed.