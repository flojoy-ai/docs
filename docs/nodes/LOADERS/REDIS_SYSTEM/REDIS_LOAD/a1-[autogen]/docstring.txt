The REDIS_LOAD node loads data directly from REDIS.

    Parameters
    ----------
    referred_node: str
        The node where REDIS data will be loaded from.

    Returns
    -------
    OrderedPair
