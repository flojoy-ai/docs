from flojoy import OrderedPair, flojoy
import scipy.stats


@flojoy(node_type='default')
def YEOJOHNSON(
	default: OrderedPair,
	lmbda: float,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.stats.yeojohnson(
			x=default.y,
			lmbda=lmbda,
		)
	)
	return result
