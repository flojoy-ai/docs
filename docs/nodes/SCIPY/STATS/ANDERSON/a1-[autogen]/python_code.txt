from flojoy import OrderedPair, flojoy
import scipy.stats


@flojoy(node_type='default')
def ANDERSON(
	default: OrderedPair,
	dist: str = 'norm',
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.stats.anderson(
			x=default.y,
			dist=dist,
		)
	)
	return result
