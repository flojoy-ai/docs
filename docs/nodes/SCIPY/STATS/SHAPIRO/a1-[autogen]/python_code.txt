from flojoy import OrderedPair, flojoy
import scipy.stats


@flojoy(node_type='default')
def SHAPIRO(
	default: OrderedPair,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.stats.shapiro(
			x=default.y,
			)
	)
	return result
