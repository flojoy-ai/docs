from flojoy import OrderedPair, flojoy
import scipy.signal


@flojoy(node_type='default')
def CUBIC(
	default: OrderedPair,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.signal.cubic(
			x=default.y,
			)
	)
	return result
