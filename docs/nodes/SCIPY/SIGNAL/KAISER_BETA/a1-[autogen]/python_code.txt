from flojoy import OrderedPair, flojoy
import scipy.signal


@flojoy(node_type='default')
def KAISER_BETA(
	default: OrderedPair,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.signal.kaiser_beta(
			a=default.y,
			)
	)
	return result
