from flojoy import OrderedPair, flojoy
import scipy.signal


@flojoy(node_type='default')
def QUADRATIC(
	default: OrderedPair,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.signal.quadratic(
			x=default.y,
			)
	)
	return result
