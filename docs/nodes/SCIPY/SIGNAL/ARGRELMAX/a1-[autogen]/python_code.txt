from flojoy import OrderedPair, flojoy
import scipy.signal


@flojoy(node_type='default')
def ARGRELMAX(
	default: OrderedPair,
	axis: int = 0,
	order: int = 1,
	mode: str = 'clip',
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.signal.argrelmax(
			data=default.y,
			axis=axis,
			order=order,
			mode=mode,
		)
	)
	return result
