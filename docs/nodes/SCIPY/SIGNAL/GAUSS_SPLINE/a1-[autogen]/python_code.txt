from flojoy import OrderedPair, flojoy
import scipy.signal


@flojoy(node_type='default')
def GAUSS_SPLINE(
	default: OrderedPair,
	n: int,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.signal.gauss_spline(
			x=default.y,
			n=n,
		)
	)
	return result
