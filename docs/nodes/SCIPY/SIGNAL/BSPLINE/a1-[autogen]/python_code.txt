from flojoy import OrderedPair, flojoy
import scipy.signal


@flojoy(node_type='default')
def BSPLINE(
	default: OrderedPair,
	n: int,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=scipy.signal.bspline(
			x=default.y,
			n=n,
		)
	)
	return result
