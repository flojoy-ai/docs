from flojoy import OrderedPair, flojoy
import numpy.linalg


@flojoy(node_type='default')
def EIGVALSH(
	default: OrderedPair,
	UPLO: str = 'L',
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=numpy.linalg.eigvalsh(
			a=default.y,
			UPLO=UPLO,
		)
	)
	return result
