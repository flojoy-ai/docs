from flojoy import OrderedPair, flojoy
import numpy.linalg


@flojoy(node_type='default')
def INV(
	default: OrderedPair,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=numpy.linalg.inv(
			a=default.y,
			)
	)
	return result
