from flojoy import OrderedPair, flojoy
import numpy.linalg


@flojoy(node_type='default')
def MATRIX_POWER(
	default: OrderedPair,
	n: int,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=numpy.linalg.matrix_power(
			a=default.y,
			n=n,
		)
	)
	return result
