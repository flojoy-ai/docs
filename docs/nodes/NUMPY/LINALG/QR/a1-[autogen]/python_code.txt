from flojoy import OrderedPair, flojoy
import numpy.linalg


@flojoy(node_type='default')
def QR(
	default: OrderedPair,
	mode: str = 'reduced',
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=numpy.linalg.qr(
			a=default.y,
			mode=mode,
		)
	)
	return result
