from flojoy import OrderedPair, flojoy
import numpy.linalg


@flojoy(node_type='default')
def EIG(
	default: OrderedPair,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=numpy.linalg.eig(
			a=default.y,
			)
	)
	return result
