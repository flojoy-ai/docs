from flojoy import DataContainer, flojoy
import numpy.linalg


@flojoy
def EIG(dc, params):
    
    return DataContainer(
        x=dc[0].y,
        y=numpy.linalg.eig(
            a=dc[0].y,
        ),
    )
