from flojoy import OrderedPair, flojoy
import numpy.linalg


@flojoy(node_type='default')
def EIGVALS(
	default: OrderedPair,
	) -> OrderedPair:
	''''''
	result = OrderedPair(
		x=default.x,
		y=numpy.linalg.eigvals(
			a=default.y,
			)
	)
	return result
